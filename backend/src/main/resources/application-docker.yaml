spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/url_shortener?currentSchema=uss
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

app:
  server-id: 1